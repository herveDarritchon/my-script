# list les device usb
echo "Liste les device usb : diskutil list"

# mount un device usb
echo "Mount un device usb de la liste : diskutil mount /dev/disk4s1"

#unmound un device usb
echo "Unmount un device usb : diskutil unmount /dev/disk4s1"
